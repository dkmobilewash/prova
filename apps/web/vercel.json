{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @prova/db run check:schema && pnpm turbo run build --filter=@prova/web",
  "crons": [
    {
      "path": "/api/notifications/digest",
      "schedule": "0 13 * * *"
    }
  ]
}
