// Type surface for fakeBlobToken.mjs — the same pattern as
// packages/db/scripts/connection-target.d.mts, for the same reason: the
// .mjs is shared with plain-Node e2e/run.mjs, and the TypeScript importers
// need a declaration to typecheck against.
export const E2E_BLOB_STORE_ID: string;
export const E2E_FAKE_BLOB_TOKEN: string;
